name: patania_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.14.0 # O la versión más reciente que `flutterfire configure` haya sugerido
  firebase_auth: ^5.6.0 # O la versión más reciente
  cloud_firestore: ^5.6.9 # O la versión más reciente
  cupertino_icons: ^1.0.2


  english_words: ^4.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
